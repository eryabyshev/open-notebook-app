# App icons for electron-builder

- `icon.png` — generated by `desktop/generate_app_icon.py` (512×512)
- electron-builder converts it to `.icns` / `.ico` at pack time

Regenerate:

```bash
uv run python desktop/generate_app_icon.py
```

Replace `icon.png` with a branded asset when ready for release.
